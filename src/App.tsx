import EducationNetworkCalculator from './education-network-calculator';

function App() {
  return (
    // The component uses a white background, so let's set a base for the page
    <div className="bg-gray-100 min-h-screen">
      <EducationNetworkCalculator />
    </div>
  );
}

export default App;
